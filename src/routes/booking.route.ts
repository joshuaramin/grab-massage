import express from "express";

const router = express.Router();

router.get("/getBooking", () => {});
router.post("/createBooking", () => {});
router.put("/updateBooking/:id", () => {});
router.patch("/update/:id", () => {});

export default router;
